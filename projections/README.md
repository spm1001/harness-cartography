# Projections — generated, never hand-edited

Each dated PNG + cells.md pair is one `score` run over the then-current run
files. Regenerate rather than edit; the cell tables carry the judgement flags.
Label layout is crude v1 — coincident points dodge vertically; labels can
still crowd. Browser-line surfaces are administrator-supplied on the CLI.
