# Understanding

Kinetic layer for the harness-cartography practice: same model, different container — what does the container grant, withhold, tax, or wall off? The knowledge (instrument, runs, frame) lives in `~/notes/practices/harness-cartography/`; this repo holds the code that administers and scores.

## The frame, compressed (full version in the notes room)

- **Two thresholds:** the shell line (capabilities become constructible, not enumerable) and the browser line (agent borrows the human's logged-in hands). Two axes: Freedom (primitives) vs Furniture (pre-built polish).
- **Behavioural probes, not self-report** — the letter (instrument.md §5) makes subjects attempt each item; self-report error is bidirectional.
- **Re-runnability beats depth.** Surfaces change monthly; the map is a film. That's why the scoring pipeline matters: three parseable runs existed before this repo did (2026-08-15: cowork-local, cowork-cloud, cowork-chrome, plus stock Code), and every future run should score mechanically.

## Why this repo minted (2026-08-23)

The Toolmaking queue line: scaffold the runner repo, then Gemini Notebook probe + letter v2. Scaffolded during a loop session; canary sweep the same evening verified all four planted canaries, so the persistence cells for the 2026-08-15 quartet are now measured-verified.

## Decisions

- **2026-08-23, prefix `cart`.** Room-side tools (canary-sweep, room-census) stay in notes until deliberately migrated — probe subjects minted them there and the room's Status cites them.
